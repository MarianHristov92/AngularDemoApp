import { HomePageFeatureListViewModel } from './home-page-feature-list-view-model';

describe('HomePageFeatureListViewModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new HomePageFeatureListViewModel()).toBeTruthy();
  });
});
